# Canonical Selection Verification

{% include "runtime/partial/step_verification_contract.md.j2" %}

Read `{{ input_path }}`, `{{ step_result_path }}`, and accepted rows through `brand-size-chart-source-discovery-read {{ input_path }} <source_type> list-accepted` for each table-available source. Read charts and referenced evidence without writing artifacts.

Verify that each tied highest-priority selection is semantically equivalent across chart content, market scope, evidence, and provenance, and that each omitted group has a real semantic disagreement. Do not repeat accepted-row membership, priority, representative order, or derived-gap checks owned by the mechanical validator.
