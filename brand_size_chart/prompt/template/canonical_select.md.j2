# Canonical Select

Select one canonical chart per size group from the supplied draft result only. Do not add a `size_group_key`, `selected_source_type`, or `selected_source_url` that is absent from the supplied draft result. Prefer the highest numeric `source_priority` for the same `size_group_key` and record unresolved conflicts with all compared decision values.

{% include "partial/size_group_key_contract.md.j2" %}

{% include "partial/stage_retry_context.md.j2" %}
