# Canonical Selection

{% include "runtime/partial/step_retry_context.md.j2" %}

Read `BrandSourceTypeResultStepInput` from `{{ input_path }}`. For every complete table-available source result, run `brand-size-chart-source-discovery-read {{ input_path }} <source_type> list-accepted`. Group the transient rows by `source_table.size_group_key` and keep only their highest `source_priority`.

Select the sole highest-priority chart. For tied highest-priority rows, compare chart semantics, market scope, evidence, and provenance. When they are equivalent select the deterministic first by `(source_table.market_scope_key, source_table.source_url, source_table.source_title)`; when they disagree omit the group. Return only `CanonicalSelectionActionOutput`. Do not write SQLite, charts, or query views.
