# Coverage Decision Verification

{% include "runtime/partial/step_verification_contract.md.j2" %}

Read `{{ input_path }}`, `{{ step_result_path }}`, and accepted rows through `brand-size-chart-source-discovery-read {{ input_path }} <source_type> list-accepted` for each table-available source. Read referenced charts and evidence without writing files.

Verify positive product coverage from chart semantics, description, row evidence, and source provenance. Reject weak path or identifier-only claims. Verify gaps remain unsupported after all accepted tables are considered. Do not repeat partition, duplicate, accepted-chart membership, or path checks owned by the mechanical validator.
